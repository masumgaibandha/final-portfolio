# Contact form delivery (src/app/api/contact/route.ts).
# Get an access key from https://web3forms.com — it is tied to your inbox.
# Without this the form returns 503 and tells visitors to email directly.
# Copy this file to `.env` and fill the value in there. Never commit `.env`.
WEB3FORMS_ACCESS_KEY=
